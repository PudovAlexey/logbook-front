import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    wrapper: {
        padding: 24,
        alignSelf: 'stretch',
        flex: 1,
        justifyContent: 'center',
    },
});

export {
    styles,
};
