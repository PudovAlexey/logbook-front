import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    wrapper: {
        position: 'relative',
    },

    messageContainer: {
        position: 'absolute',
        bottom: 70,
        left: 0,
        right: 0,
    }
})

export {
    styles
}
