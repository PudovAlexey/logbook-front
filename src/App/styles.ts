import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    space: {
        display: 'flex',
        flexDirection: 'column',
        width: '100%',
    },

    header: {
      flex: 0.05,  
    }
})

export {
    styles
}